# Nivalis melee combat compound task. Mirrors the standard MeleeCombatCompound but uses the
# NivalisMeleeOperator so NPCs swing Nivalis melee weapons (spear/knife/pipe/ice-axe/cleaver) which
# don't carry a regular MeleeWeapon component.
- type: htnCompound
  id: NivalisMeleeCombatCompound
  branches:
    - tasks:
        - !type:HTNPrimitiveTask
          operator: !type:UtilityOperator
            proto: NearbyMeleeTargets
        - !type:HTNCompoundTask
          task: NivalisBeforeMeleeAttackCompound

- type: htnCompound
  id: NivalisBeforeMeleeAttackCompound
  branches:
    - preconditions:
        - !type:BuckledPrecondition
          isBuckled: true
      tasks:
        - !type:HTNPrimitiveTask
          operator: !type:UnbuckleOperator
            shutdownState: TaskFinished

    - preconditions:
        - !type:PulledPrecondition
          isPulled: true
      tasks:
        - !type:HTNPrimitiveTask
          operator: !type:UnPullOperator
            shutdownState: TaskFinished

    - preconditions:
        - !type:InContainerPrecondition
          isInContainer: true
      tasks:
        - !type:HTNCompoundTask
          task: EscapeCompound

    # Nivalis melee attack toward target.
    - tasks:
        - !type:HTNCompoundTask
          task: NivalisMeleeAttackTargetCompound

# Tries to Nivalis melee attack our target.
- type: htnCompound
  id: NivalisMeleeAttackTargetCompound
  branches:
    - preconditions:
      - !type:KeyExistsPrecondition
        key: Target
      tasks:
        - !type:HTNPrimitiveTask
          operator: !type:MoveToOperator
            shutdownState: PlanFinished
            pathfindInPlanning: true
            removeKeyOnFinish: false
            targetKey: TargetCoordinates
            pathfindKey: TargetPathfind
            rangeKey: MeleeRange
        - !type:HTNPrimitiveTask
          operator: !type:NivalisMeleeOperator
            targetKey: Target
          preconditions:
            - !type:KeyExistsPrecondition
              key: Target
            - !type:TargetInRangePrecondition
              targetKey: Target
              rangeKey: MeleeRange
          services:
            - !type:UtilityService
              id: MeleeService
              proto: NearbyMeleeTargets
              key: Target

# Ranged combat for scavenger gunners. Like RangedCombatCompound but WITHOUT the branch that
# drops the gun when empty: the scavenger pistol recharges its own ammo, so the gunner holds onto
# it and simply waits a couple of seconds to fire again.
- type: htnCompound
  id: NivalisGunnerCombatCompound
  branches:
    # Rack the bolt if the gun needs it.
    - preconditions:
        - !type:ActiveHandComponentPrecondition
          components:
            - type: Gun
        - !type:NeedToRackBoltPrecondition
      tasks:
        - !type:HTNPrimitiveTask
          operator: !type:RackBoltOperator

    # Wield gun.
    - preconditions:
        - !type:ActiveHandComponentPrecondition
          components:
            - type: Gun
        - !type:WieldedPrecondition
          wielded: false
      tasks:
        - !type:HTNPrimitiveTask
          operator: !type:WieldOperator

    # Move to the target and shoot if we have charge.
    - preconditions:
        - !type:GunAmmoPrecondition
          minPercent: 0.001
      tasks:
        - !type:HTNCompoundTask
          task: GunCombatCompound

    # Pick up a gun if we have no gun in hand.
    - preconditions:
        - !type:ActiveHandComponentPrecondition
          components:
            - type: Gun
          invert: true
      tasks:
        - !type:HTNCompoundTask
          task: PickupGunCompound

# Root task for Nivalis melee scavengers. Keeps the gunner combat branch available so
# scavengers that are given a firearm can fall back to shooting.
- type: htnCompound
  id: NivalisScavengerHostileCompound
  branches:
    - tasks:
        - !type:HTNCompoundTask
          task: NivalisGunnerCombatCompound
    - tasks:
        - !type:HTNCompoundTask
          task: NivalisMeleeCombatCompound
    - tasks:
        - !type:HTNCompoundTask
          task: IdleCompound
